{
  "name": "AI CLI Cloud",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {},
    "ghcr.io/devcontainers/features/python:1": {}
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y pipx && pipx ensurepath && /bin/bash -lc 'pipx install llm && llm && pipx inject llm llm-anthropic llm-google-genai'"
}
